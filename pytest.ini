[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: requires running Ollama + Postgres (skipped by default)
addopts = -ra -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
